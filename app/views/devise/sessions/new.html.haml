%h2 Iniciar sesión
= form_for @user, validate: true do |f|
  .field
    = f.label :name
    %br/
    = f.text_field :name
  .field
  - if devise_mapping.rememberable?
    .field
      = f.check_box :remember_me
      = f.label :remember_me
  %div
  = link_to "Iniciar sesion", users_path
= render "devise/shared/links"
